<!DOCTYPE html>
<html lang="en">

<head>
  <!DOCTYPE html>
  <html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Order Failed</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <script>
      window.onload = function(){
        let orderId = localStorage.getItem('oid');
       orderId=  orderId.slice(1,-1)
        console.log(document.getElementById('failed').href =`/orderDetail?id=${orderId}` )
        console.log(orderId);
      }
   </script>
    <style>
      body {
        background-color: #f8f9fa;
        font-family: Arial, sans-serif;
      }
  
      .container {
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
      }
  
      .order-details {
        max-width: 600px;
        padding: 30px;
        border-radius: 10px;
        box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
        background-color: #ffffff;
        text-align: center;
      }
  
      .order-details h2 {
        color: #dc3545;
        font-size: 36px;
        margin-bottom: 20px;
      }
  
      .order-details p {
        font-size: 20px;
        margin-bottom: 30px;
      }
  
      .cross-mark {
        color: #dc3545;
        font-size: 64px;
        margin-bottom: 30px;
      }
  
      .button-group {
        display: flex;
        justify-content: center;
        gap: 20px;
      }
  
      .btn-retry, .btn-home {
        padding: 12px 24px;
        font-size: 18px;
        color: #ffffff;
        border: none;
        border-radius: 5px;
        transition: background-color 0.3s ease;
      }
  
      .btn-retry {
        background-color: #28a745;
      }
  
      .btn-home {
        background-color: #007bff;
      }
  
      .btn-retry:hover {
        background-color: #218838;
      }
  
      .btn-home:hover {
        background-color: #0056b3;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="order-details">
        <div class="cross-mark">&#10060;</div>
        <h2>Order Failed</h2>
        <p>We're sorry, but there was an issue with your order. Please try again or contact support if the issue persists.</p>
        <div class="button-group">
          <a href="/" class="btn btn-home">Back to Home</a>
          <a href="" class="btn btn-retry" id="failed">Retry Payment</a>
        </div>
      </div>
    </div>
  </body>

